# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
pydantic-settings>=2.6.0
email-validator>=2.0.0
bcrypt>=4.2.0

# Agents / LLM
langchain-core>=0.3.0
langchain-openai>=0.2.0

# MongoDB Atlas (driver + vector helpers)
pymongo>=4.10.0
motor>=3.6.0

# JWT (HS256 API tokens)
python-jose[cryptography]>=3.3.0
httpx>=0.27.0

# Lava & external HTTP (REST; no official Python SDK)
# Use httpx from FastAPI stack; keys in env

# Dev
ruff>=0.8.0
pytest>=8.3.0
